# Batch Demo Narration

1. Verbatim now supports bounded folder-to-folder transcription inside one approved local workspace.
2. The operator chooses relative input and output folders; the service cannot browse outside the configured workspace root.
3. This synthetic demonstration selects TXT, Markdown, SRT, VTT, and JSON evidence.
4. One explicit authorization applies to every MP4 directly inside the input folder.
5. Two files enter the same bounded local validation and Whisper pipeline; failures stay isolated per file.
6. Ten selected outputs and a provenance manifest are written without replacing existing files.
7. Each transcript remains reviewable beside its source and deterministic analysis.
8. Managed copies can be removed explicitly; original input and output files remain untouched.
9. The batch record and managed jobs are deleted; the controlled synthetic demonstration is complete.

The video visibly labels the processing interval played at 16×. The unedited measured processing time remains in the evidence report.
